# 04 - Reutilização (SRP + DRY)

Foco em evitar duplicação de regra de imposto.

- `ruim.java`: mesma regra duplicada em classes diferentes
- `bom.java`: contrato reutilizável e implementação única
